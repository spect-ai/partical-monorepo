{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/application-dashboard",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/application-dashboard",
        "outputPath": "dist/apps/application-dashboard"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "application-dashboard:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "application-dashboard:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "application-dashboard:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "application-dashboard:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/application-dashboard"],
      "options": {
        "jestConfig": "apps/application-dashboard/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/application-dashboard/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
