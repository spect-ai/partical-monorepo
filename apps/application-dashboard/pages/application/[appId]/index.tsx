import { Layout } from '@partical/common';
import AppDashboard from '../../../app/modules/appDashboard';

export function AppId() {
  return (
    <Layout>
      <AppDashboard />
    </Layout>
  );
}

export default AppId;
